#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "main.h"
/* more headers goes there */

/**
* Return: returns 0 if successful
*/
void positive_or_negative(int i)
{
if (i == 0)
{
printf("%d is zero\n", i);
}
else if (i == 16)
{
	printf("%d\n", i);
}
}
