#include "main.h"
/**
 * _isupper - function to check if letter is uppercase
 * @x: To be checked
 * Return: 1 for upper letter, 0 otherwise
 */

int _isupper(int x)
{
x = 0;
if (x >= 65 && x <= 90)
{
	return (0);
}
return (0);
}
