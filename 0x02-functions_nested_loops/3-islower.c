#include "main.h"
/**
 *_islower - function to print lowercase
 *Return: returns 1 if lowercase, else 1
 *@c: parameter to be printed
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
